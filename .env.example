# Public build-time config. `output: export` inlines these at build time, so CI must set them
# before `next build`.

# FastAPI backend origin. Dev runs on :5173 because the backend's CORS allowlist contains it.
NEXT_PUBLIC_API_URL=http://localhost:8000
